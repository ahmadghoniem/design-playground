{
  "name": "design-playground",
  "version": "0.0.0",
  "private": true,
  "description": "Local-dev design playground. Its dependencies install NESTED under src/app/playground/node_modules (via setup.mjs) so the host project's package.json and lockfile are never touched. Local-dev only — never built in CI/prod. Vite-compatible. react/react-dom/tailwindcss are provided by the host (peerDependencies).",
  "dependencies": {
    "@hono/node-server": "^1.19.14",
    "@radix-ui/react-alert-dialog": "^1.1.16",
    "@radix-ui/react-dialog": "^1.1.16",
    "@radix-ui/react-slot": "^1.2.5",
    "@radix-ui/react-tooltip": "^1.2.9",
    "@tailwindcss/typography": "^0.5.20",
    "@xyflow/react": "^12.11.0",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "hono": "^4.12.27",
    "lucide-react": "^1.17.0",
    "sonner": "^2.0.7",
    "tailwind-merge": "^3.6.0",
    "tw-animate-css": "^1.4.0",
    "typescript": "5",
    "zustand": "^5.0.14"
  },
  "peerDependencies": {
    "react": "^18 || ^19",
    "react-dom": "^18 || ^19",
    "tailwindcss": "^4",
    "vite": "^5 || ^6"
  },
  "scripts": {
    "check:boundaries": "bunx --bun dependency-cruiser@16 --config .dependency-cruiser.cjs --ts-config tsconfig.json \"app/**/*.{ts,tsx}\" \"features/**/*.{ts,tsx}\" \"shared/**/*.{ts,tsx}\" \"server/**/*.{ts,tsx}\" registry.tsx registry-types.ts dev-entry.tsx",
    "check:knip": "bunx knip --config knip.json",
    "check:architecture": "bun scripts/verify-architecture.mjs"
  }
}
